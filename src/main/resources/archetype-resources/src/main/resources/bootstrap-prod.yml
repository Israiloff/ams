spring:
  cloud:
    consul:
      config:
        fail-fast: true
        format: yaml
        profile-separator: '-'
        enabled: true
        watch:
          enabled: false
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 100
